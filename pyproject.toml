[project]
name = "hk-mobile-broadband"
version = "0.1.0"

dependencies = [
    "flask",
    "pandas",
    "requests",
    "gunicorn",
    "flask-limiter"
]

[tool.vercel]
entrypoint = "app:app"

